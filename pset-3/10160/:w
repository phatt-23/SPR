import sys

if __name__ == "__main__":
    lines = [line.strip() for line in sys.stdin.readlines() if line.strip() != '']

    i = 0
    while i < len(lines):
        n, m = (int(num) for num in lines[i])

        assert 3 <= n <= 35

        
